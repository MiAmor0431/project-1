import React from "react";

const Footer = () => (
    <footer className="text-center py-3 mt-5 text-muted border-top">
        <small>&copy; {new Date().getFullYear()} SatuBooster</small>
    </footer>
);

export default Footer;